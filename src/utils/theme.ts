import { createTheme } from "@mui/material/styles";

export const appTheme = createTheme({
  palette: {
    primary: {
      main: "#05662b",
      contrastText: "#fff"
    },
    secondary: {
      main: "#7dde21",
      contrastText: "#05662b"
    }
  }
});
